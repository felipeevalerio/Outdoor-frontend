import styled from "styled-components";
import { Form } from "../../../../components/Form";

export const CreatePostModalContainer = styled(Form)`
`

export const InputsCreatePostContainer = styled.div`
    display: flex;
    flex-direction: column;
    flex-wrap: wrap;
    max-height: 320px;
    gap: 1rem;
`